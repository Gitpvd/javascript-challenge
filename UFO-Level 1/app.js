// from data.js
var tbody = d3.select("tbody");

var tableData = data;

console.log(data);

// // Step 1: Loop Through `data` and console.log each siting report object
data.forEach(function(tableData) {
    console.log(tableData);
    });

